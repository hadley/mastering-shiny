# Working with data

* Static
    * Preprocessing data in external processes
    * Loading in global scope vs. per-session scope
* Manual refresh with `eventReactive`
* Reactive (`reactiveFileReader`, `reactivePoll`)
* Working with databases
    * Inserting data with parameterized queries
    * Connection pooling
